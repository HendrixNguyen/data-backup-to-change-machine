# Deliberate exceptions to the export leak scan: <path-substring> <pattern-name>  # why
# Each line is a reviewed claim that a match is NOT a credential. Keep the reason specific.

harness/skills/browse/auth-browse.sh        google-api-key  # Firebase Web API key: a public client identifier, not a secret
harness/skills/browse/get-firebase-token.sh google-api-key  # same, used to call the identitytoolkit endpoint
